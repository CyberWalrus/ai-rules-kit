---
id: 07-agent-mode-workflow
type: compact
alwaysApply: true
---

# Agent Mode Workflow

<agent_mode_workflow>

**EXECUTE:** Apply when system_reminder contains "Agent mode" / "Debug mode" / unset. Skip for Plan/Ask mode.

**ВАЖНО: Все ответы должны быть на русском языке.**

**NOTE:** Mode announcement in `.cursor/rules/01-chat-mode-router.mdc`. If plan exists → follow todos.

**🚨 ENFORCEMENT:**

1. Before EACH step → verbalize: "Checking step N: [condition] → [result]"
2. Skip step = INSTANT VIOLATION → restart from step 1
3. Uncertain about rule? → Re-read rule text → Apply literally
4. "Task is simple" / "I know this" = WRONG REASONING → Follow protocol EXACTLY

**⚡ MOTIVATION:**

- ✅ Correct detection → smooth execution, trust
- ❌ Skip protocol → immediate failure, wasted tokens, rework

**ANTI-SHORTCUTS:**

"Simple task, skip check" → WRONG! Complexity irrelevant.
"Faster without announcement" → WRONG! Speed without protocol = failure.
"system_reminder shows mode" → WRONG! Marker check MANDATORY.
"read_lints passed = MCP done" → WRONG! read_lints ≠ MCP validation.

**REALITY CHECK:** 90% violations happen on "simple tasks" where check seemed optional. It's NEVER optional.

**🚨 DOCS (read BEFORE modify):**

**Triggers:** FORBIDDEN to modify until docs read.

- ANY `.md/.mdc` → `.cursor/rules/prompt-structure-guide.mdc`
- `*.test.ts` → `.cursor/rules/testing.md`
- New `.ts/.tsx` → `.cursor/rules/architecture.md`, `.cursor/rules/naming.md`
- Edit `.ts/.tsx` (≥20 lines) → `.cursor/rules/code-standards.md`, `.cursor/rules/naming.md`
- AI docs → `.cursor/rules/ai-docs-workflow.mdc`

**AI DOCS TRIGGERS:**

1. New module/package → CREATE module-ai-docs.md / package-ai-docs.md
2. Changed public API (signatures, types) → UPDATE contract section
3. Added/removed dependency → UPDATE dependencies section
4. Fixed behavior bug → UPDATE edge_cases section
5. Refactoring without API change → DO NOT update

**Exceptions:** README.md, CHANGELOG.md, `*.config.ts`

**Self-check:** Before editing `.md/.mdc` → inspect YAML → if `type:` exists → prompt file → trigger applies

**EXTERNAL:**

1. Context7: Before package → resolve-library-id + get-library-docs
2. Web Search: Error persists 2+ attempts → search → retry

**TESTS:**

- Unit: All `.ts/.tsx` need tests in `__tests__/` (except `*.types.ts`, `*.constants.ts`, `*.schema.ts`)
- E2E: New/modified features → e2e test in `src/__tests__/e2e/` with mock data

**🚨⛔ VALIDATION — BLOCKING (after modifications):**

**REQUIRED:**

- `.ts/.tsx` → `code`
- `*.test.ts` → `tests`
- `.mdc/.md` → `prompts`
- `architecture.xml` → `architecture`
- AI docs → `documentation`

**SKIP:** README.md, CHANGELOG.md, `*.config.ts`, `package.json`, `tsconfig.json`

**Steps (CRITICAL - PARALLEL EXECUTION):**

1. **PARALLEL:** Batch ALL changed files → call MCP validator in PARALLEL (one tool call per file, NOT sequential)
2. Wait for ALL parallel results → Target ≥85 per file
3. If any file <85 → fix → re-validate that file (max 3x)
4. ALL files ≥85 → THEN run `yarn lint` (0 errors REQUIRED)

**WHY PARALLEL:** Sequential validation = N × latency. Parallel = 1 × latency. Always batch MCP calls.

**ZERO TOLERANCE:** MCP validation is NON-NEGOTIABLE. Never skip, never postpone. 3 failures → escalate.

**WORKFLOW:**

1. Check plan → follow todos
2. Check triggers → read docs
3. External deps → Context7/web search
4. Execute → functional style, unit tests
5. E2E tests → new/modified features with mocks
6. ⛔ Validate ALL → MCP batch in PARALLEL (≥85) — MUST COMPLETE BEFORE step 7
7. Final → run `yarn lint` (0 errors REQUIRED)
8. AI docs → check AI DOCS TRIGGERS 1-4 → create/update if matched
9. Report → files with scores

**PRE-RESPONSE BARRIER:** Verify ALL before responding:

- [ ] Required docs read (if triggers matched)
- [ ] All files validated via MCP (≥85)
- [ ] Unit tests written/updated
- [ ] E2E tests for new features
- [ ] Lint/type/test: 0 errors
- [ ] AI docs created/updated (if AI DOCS TRIGGERS 1-4 matched)

❌ ANY unchecked → FORBIDDEN to respond | ✓ ALL checked → proceed

**FALLBACKS:** MCP unavailable → lint/type/test | Validation <85 → iterate 3x | Unclear → ask

<completion_criteria>
All validated (≥85), e2e tests pass, lint/type/test: 0 errors, todos done.

Format: `[OK] Modified [N] | Validated [N] | ≥85: YES | E2E: PASS | Errors: 0 | COMPLETE`
</completion_criteria>

</agent_mode_workflow>
